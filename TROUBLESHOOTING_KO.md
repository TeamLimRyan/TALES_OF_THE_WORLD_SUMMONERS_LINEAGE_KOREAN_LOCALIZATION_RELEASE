# 문제 해결

## 원본 해시 불일치

패치를 적용하지 마십시오. 이미 수정된 ROM, 다른 리비전, 헤더가 추가된 파일일 수 있습니다. 지원되는 원본 SHA-256은 `b41c293f...6186cfdd3`입니다.

## xdelta가 원본을 찾지 못함

경로에 공백이나 작은따옴표가 있으므로 전체 경로를 따옴표로 감싸십시오. 자동 적용 스크립트를 쓰면 인수 목록으로 안전하게 전달됩니다.

## `target window checksum mismatch` 또는 유사 오류

대부분 원본 ROM이 다르거나 패치 파일이 손상된 경우입니다.

1. 원본 SHA-256을 확인합니다.
2. 패치 SHA-256이 `550aa8ac...57157ba11`인지 확인합니다.
3. 새 출력 파일명으로 다시 적용합니다.

## 패치 결과 해시 불일치

정상 결과 SHA-256은 `43c315b2...6b8860be4`입니다. 자동 적용 스크립트는 불일치 출력물을 최종 파일로 남기지 않습니다.

## 글자가 흐리거나 두꺼워 보임

에뮬레이터의 비정수 배율, 선형 필터링, 셰이더를 끄고 정수 배율과 최근접 보간을 사용해 비교하십시오. 실제 글리프는 GBA 네이티브 픽셀 그리드에 맞춰 제작됐습니다.

## 이름 입력 방법

화면의 한글 자모를 선택하면 초성·중성·종성이 조합됩니다. 테스트 기준 이름은 `임라이언`이며, 지우기는 화면 버튼 또는 B 버튼을 사용합니다.

## 그래도 해결되지 않음

[SUPPORT_KO.md](SUPPORT_KO.md)의 필수 정보를 포함해 GitHub Issue를 작성하십시오. ROM, 세이브, BIOS는 올리지 마십시오.
